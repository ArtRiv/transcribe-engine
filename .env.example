# transcribe-engine — environment variables
#
# v0.1.0 ships with no required env vars.
#
# Phase 8 will add: TRANSCRIBE_SIGNALING_URL (signaling Vercel function URL).
# Until then, this file is intentionally empty.
